---
phase: 01-foundation-project-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
---

<objective>
Initialize iOS project with Xcode, configure Swift Package Manager dependencies, and establish basic project structure.

Purpose: Create the foundation iOS app project with modern Swift tooling
Output: Working Xcode project with dependencies configured and basic app structure
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <action>Verify Xcode and iOS development environment setup</action>
  <instructions>
    I detected Swift 6.1.2 is installed, but Xcode command-line tools may not be configured.

    Please verify:
    1. Xcode is installed (version 15.0 or later for iOS 17+ support)
    2. Run: xcode-select --install (if needed)
    3. Run: xcodebuild -version (should show Xcode version)

    If Xcode is not installed, download from Mac App Store or developer.apple.com
  </instructions>
  <verification>xcodebuild -version returns Xcode version successfully</verification>
  <resume-signal>Type "done" when Xcode is ready</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Create iOS app project with SwiftUI</name>
  <files>MeetingManager.xcodeproj, MeetingManager/MeetingManagerApp.swift, MeetingManager/ContentView.swift</files>
  <action>
    Create new iOS app project using command-line (since we're in CLI environment):

    1. Create project directory: MeetingManager
    2. Initialize as iOS app with SwiftUI:
       - Minimum iOS version: 17.0 (modern SwiftUI features, async/await)
       - Use SwiftUI for UI framework
       - Use Swift 6.1+ language version
    3. Create Package.swift for Swift Package Manager dependency management
    4. Create basic app entry point (App struct) and initial ContentView
    5. Configure .gitignore for Xcode projects (xcuserdata, DerivedData, .DS_Store)

    Project structure:
    ```
    MeetingManager/
      ├── MeetingManager.xcodeproj/
      ├── MeetingManager/
      │   ├── MeetingManagerApp.swift (app entry point)
      │   ├── ContentView.swift (temporary main view)
      │   ├── Assets.xcassets/
      │   └── Info.plist
      ├── Package.swift (SPM dependencies)
      └── .gitignore
    ```

    Do NOT use Xcode GUI - create project structure programmatically or using xcodebuild.
  </action>
  <verify>
    - MeetingManager.xcodeproj exists
    - xcodebuild -project MeetingManager/MeetingManager.xcodeproj -list shows scheme
    - Package.swift exists and is valid
  </verify>
  <done>iOS project builds successfully with xcodebuild, basic app structure in place</done>
</task>

<task type="auto">
  <name>Task 2: Configure Swift Package Manager dependencies</name>
  <files>Package.swift</files>
  <action>
    Add essential dependencies via Swift Package Manager in Package.swift:

    Dependencies to add:
    - No external dependencies yet for Phase 1 (keep it minimal)
    - Will add auth, networking, AI SDKs in later phases

    For now, Package.swift should declare the project as a package but with minimal dependencies.

    Note: We're using SPM (not CocoaPods) for modern Swift dependency management.
  </action>
  <verify>
    - Package.swift is valid Swift
    - swift package resolve succeeds (if dependencies added)
  </verify>
  <done>Package.swift configured for future dependency management</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Basic iOS app project with Xcode and Swift Package Manager</what-built>
  <how-to-verify>
    1. Open: MeetingManager/MeetingManager.xcodeproj in Xcode
    2. Select target: MeetingManager (should show iOS app)
    3. Build: Command+B or Product → Build
    4. Run: Command+R or Product → Run in iOS Simulator
    5. Confirm: App launches showing ContentView without errors
    6. Check: No build warnings or errors in Xcode
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] xcodebuild -project MeetingManager/MeetingManager.xcodeproj build succeeds
- [ ] App runs in iOS Simulator without errors
- [ ] Project structure matches expected layout
- [ ] .gitignore includes Xcode-specific files
</verification>

<success_criteria>

- iOS project created and builds successfully
- Swift Package Manager configured
- App launches in simulator
- Project structure is clean and follows iOS conventions
- Human verified app runs correctly
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-setup/01-01-SUMMARY.md`
</output>
