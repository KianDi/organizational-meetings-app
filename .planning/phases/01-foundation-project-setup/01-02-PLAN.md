---
phase: 01-foundation-project-setup
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified: [MeetingManager/Models/User.swift, MeetingManager/Models/Organization.swift, MeetingManager/Models/Meeting.swift, MeetingManager/Models/Task.swift]
autonomous: true
---

<objective>
Create core Swift data models for User, Organization, Meeting, and Task entities.

Purpose: Establish the foundational data structures that will be used throughout the app
Output: Swift model files with proper types, relationships, and Codable conformance
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-project-setup/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create User model</name>
  <files>MeetingManager/Models/User.swift</files>
  <action>
    Create User.swift model with:

    Properties:
    - id: UUID (unique identifier)
    - email: String (for authentication)
    - name: String (display name)
    - createdAt: Date (account creation timestamp)
    - organizationIds: [UUID] (organizations user belongs to)

    Conform to:
    - Identifiable (SwiftUI requirement)
    - Codable (for JSON serialization with backend)
    - Equatable (for comparison)

    Use Swift struct (value type) with proper initializer.
    Add basic validation helpers as needed (e.g., isValidEmail computed property).

    Create Models/ directory if it doesn't exist.
  </action>
  <verify>Project builds without errors, User type is usable in SwiftUI previews</verify>
  <done>User.swift exists with complete model definition and Codable conformance</done>
</task>

<task type="auto">
  <name>Task 2: Create Organization model</name>
  <files>MeetingManager/Models/Organization.swift</files>
  <action>
    Create Organization.swift model with:

    Properties:
    - id: UUID
    - name: String (organization name)
    - adminId: UUID (creator/leader user ID)
    - createdAt: Date
    - inviteCode: String? (optional code for joining)
    - memberIds: [UUID] (all members including admin)

    Conform to: Identifiable, Codable, Equatable

    Use Swift struct with proper initializer.
    Consider adding computed property: isAdmin(userId: UUID) -> Bool
  </action>
  <verify>Project builds, Organization type is usable</verify>
  <done>Organization.swift exists with relationships to User</done>
</task>

<task type="auto">
  <name>Task 3: Create Meeting model</name>
  <files>MeetingManager/Models/Meeting.swift</files>
  <action>
    Create Meeting.swift model with:

    Properties:
    - id: UUID
    - organizationId: UUID (which org this meeting belongs to)
    - title: String
    - scheduledAt: Date (when meeting is scheduled)
    - startedAt: Date? (when organizer started the meeting)
    - endedAt: Date? (when meeting concluded)
    - googleDocsUrl: String? (uploaded meeting minutes URL)
    - summary: String? (AI-generated summary - added in Phase 5)
    - attendeeIds: [UUID] (users who checked in)
    - createdById: UUID (organizer who created meeting)

    Computed properties:
    - isActive: Bool (started but not ended)
    - isUpcoming: Bool (scheduled in future)
    - isPast: Bool (has ended)

    Conform to: Identifiable, Codable, Equatable

    Use Swift struct. Meeting lifecycle: scheduled → started → ended.
  </action>
  <verify>Project builds, Meeting type compiles and is usable</verify>
  <done>Meeting.swift exists with proper date handling and state properties</done>
</task>

<task type="auto">
  <name>Task 4: Create Task model</name>
  <files>MeetingManager/Models/Task.swift</files>
  <action>
    Create Task.swift model with:

    Properties:
    - id: UUID
    - meetingId: UUID (which meeting generated this task)
    - organizationId: UUID (for filtering)
    - title: String (task description)
    - assigneeId: UUID? (user assigned to task, extracted by AI)
    - dueDate: Date? (deadline if mentioned)
    - isCompleted: Bool (completion status)
    - createdAt: Date
    - extractedFrom: String? (source text from meeting minutes)

    Conform to: Identifiable, Codable, Equatable

    Use Swift struct. Tasks are extracted from meeting minutes by AI (Phase 5).

    Note: "Task" is a common Swift type name - consider using TaskItem or MeetingTask to avoid conflicts with Concurrency.Task.
    Use namespace: struct MeetingTask to be explicit.
  </action>
  <verify>Project builds without naming conflicts, MeetingTask type is usable</verify>
  <done>MeetingTask.swift exists with properties for AI extraction and assignment</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] xcodebuild build succeeds
- [ ] All four model files exist in Models/ directory
- [ ] All models conform to Identifiable, Codable, Equatable
- [ ] No type naming conflicts (especially with Concurrency.Task)
- [ ] Models have proper relationships via UUID references
</verification>

<success_criteria>

- All core data models created
- Models follow Swift best practices (structs, Codable, Identifiable)
- Proper relationships between entities via UUIDs
- No compiler errors or warnings
- Models are ready for use in Views and ViewModels
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-project-setup/01-02-SUMMARY.md`
</output>
