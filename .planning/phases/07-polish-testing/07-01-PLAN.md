---
phase: 07-polish-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  MeetingManager/MeetingManager/State/MeetingState.swift,
  MeetingManager/MeetingManager/State/OrganizationState.swift,
  MeetingManager/MeetingManager/Views/**/*.swift
]
autonomous: true
---

<objective>
Implement comprehensive loading states and error handling across all views to improve user experience and resilience.

Purpose: Ensure users receive clear feedback during async operations and helpful error messages when things go wrong, creating a polished, production-ready experience.
Output: Consistent loading indicators, error alerts, and graceful degradation throughout the app.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-task-calendar-views/06-03-SUMMARY.md

# Current state analysis
Existing loading/error patterns:
- OrganizationListView has errorMessage @State and basic alerts
- MemberListView has isLoading @State with ProgressView
- MeetingState has isLoading and processingState properties
- Some views lack loading indicators during async operations
- Error handling is inconsistent across views

Need to standardize and complete the patterns.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add comprehensive loading states to MeetingState and OrganizationState</name>
  <files>MeetingManager/MeetingManager/State/MeetingState.swift, MeetingManager/MeetingManager/State/OrganizationState.swift</files>
  <action>
    For MeetingState:
    - Add @Published var error: Error? property for error tracking
    - Ensure all async methods set isLoading = true at start, defer { isLoading = false }
    - Wrap try/catch blocks to capture errors: catch { error = error; throw error }
    - Add isLoadingMeetings, isLoadingTasks as separate flags to track specific operations

    For OrganizationState:
    - Add @Published var error: Error? property
    - Add isLoadingOrganizations flag (currently exists, verify consistency)
    - Ensure all async methods follow same loading/error pattern
    - Wrap all service calls in try/catch with error assignment

    Why: Centralized error state enables consistent error display across views without prop drilling
  </action>
  <verify>Build succeeds with no errors, state properties compile correctly</verify>
  <done>Both state classes have error properties and consistent loading flag patterns</done>
</task>

<task type="auto">
  <name>Task 2: Standardize loading indicators across all major views</name>
  <files>
    MeetingManager/MeetingManager/Views/Meeting/MeetingListView.swift,
    MeetingManager/MeetingManager/Views/Task/TaskListView.swift,
    MeetingManager/MeetingManager/Views/Calendar/CalendarView.swift,
    MeetingManager/MeetingManager/Views/Meeting/MeetingDetailView.swift
  </files>
  <action>
    For each view that calls async state methods:
    - Wrap main content in ZStack with overlay ProgressView when meetingState.isLoading/organizationState.isLoading
    - Use .overlay(alignment: .center) { if state.isLoading { ProgressView() } } pattern
    - For list views: show ProgressView only when list is empty AND loading (avoid flicker on refresh)
    - For detail views: show centered ProgressView during initial load
    - Ensure pull-to-refresh doesn't show duplicate loading indicators

    Pattern to follow:
    ```swift
    ZStack {
        // Main content
        if !items.isEmpty {
            List { ... }
        } else if !isLoading {
            ContentUnavailableView(...)
        }

        // Loading overlay
        if isLoading && items.isEmpty {
            ProgressView("Loading...")
        }
    }
    ```

    Why: Consistent loading UX across the app, prevents confusion during async operations
  </action>
  <verify>Run app, navigate to each view, verify ProgressView appears during loading, disappears when complete</verify>
  <done>All major views show loading indicators consistently during async operations</done>
</task>

<task type="auto">
  <name>Task 3: Implement comprehensive error handling with user-friendly alerts</name>
  <files>
    MeetingManager/MeetingManager/Views/Meeting/MeetingListView.swift,
    MeetingManager/MeetingManager/Views/Task/TaskListView.swift,
    MeetingManager/MeetingManager/Views/Calendar/CalendarView.swift,
    MeetingManager/MeetingManager/Views/Meeting/CreateMeetingView.swift,
    MeetingManager/MeetingManager/Views/Document/DocumentPickerView.swift
  </files>
  <action>
    For each view that performs async operations:
    - Add @State private var errorMessage: String? property
    - Add .alert("Error", isPresented: .constant(errorMessage != nil)) { Button("OK") { errorMessage = nil } } message: { Text(errorMessage ?? "") }
    - Wrap async calls in do/catch blocks, set errorMessage = error.localizedDescription on catch
    - For network operations: add specific error messages like "Network connection failed. Please check your internet connection."
    - For auth errors: "Authentication failed. Please sign in again."
    - For permission errors: "You don't have permission to perform this action."

    Create user-friendly error messages instead of raw error.localizedDescription where possible.

    Handle these specific error scenarios:
    - Network failures in meeting/organization loading
    - Document upload failures (file too large, unsupported format)
    - AI processing failures (show "AI processing failed. Please try again.")
    - Meeting check-in errors (already checked in, meeting not started)

    Why: Users need clear feedback when operations fail, not cryptic technical errors
  </action>
  <verify>Trigger errors (disconnect WiFi, invalid inputs), verify friendly alerts appear with helpful messages</verify>
  <done>All views display user-friendly error alerts with actionable messages and dismiss buttons</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds with no warnings or errors
- [ ] All major views show loading indicators during async operations
- [ ] Error alerts display with user-friendly messages
- [ ] Pull-to-refresh works without flickering loading indicators
- [ ] App handles network failures gracefully
</verification>

<success_criteria>

- MeetingState and OrganizationState have error properties and consistent loading flags
- All major views (MeetingList, TaskList, Calendar, MeetingDetail) show loading indicators
- Error alerts appear consistently across views with helpful messages
- No crashes on network failures or invalid inputs
- Loading states don't flicker or show duplicate indicators
  </success_criteria>

<output>
After completion, create `.planning/phases/07-polish-testing/07-01-SUMMARY.md`
</output>
