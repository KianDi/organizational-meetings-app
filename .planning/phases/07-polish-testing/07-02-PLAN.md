---
phase: 07-polish-testing
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [
  MeetingManagerTests/MeetingManagerTests.swift,
  MeetingManagerTests/Models/MeetingTests.swift,
  MeetingManagerTests/Services/MeetingServiceTests.swift,
  MeetingManagerTests/Utilities/NameMatcherTests.swift,
  MeetingManager/MeetingManager.xcodeproj/project.pbxproj
]
autonomous: true
---

<objective>
Create comprehensive test suite covering critical business logic, services, and utilities.

Purpose: Ensure core functionality works correctly and prevent regressions as the app evolves.
Output: XCTest suite with unit tests for models, services, and utilities achieving >70% coverage of critical paths.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Current test state
- Project has 2 existing test files (likely template/placeholder)
- No test target visible in xcodebuild -list output
- Need to create test target and comprehensive test suite

Critical components to test:
- NameMatcher utility (fuzzy matching for assignees)
- Meeting model business logic (state transitions)
- MeetingService database operations (mocked)
- TaskService operations
- ProcessingState enum behavior
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test target and configure test infrastructure</name>
  <files>MeetingManager/MeetingManager.xcodeproj/project.pbxproj, MeetingManagerTests/MeetingManagerTests.swift</files>
  <action>
    Create MeetingManagerTests target if it doesn't exist:
    - Use xcodebuild or Xcode to create test target
    - Set up proper test host pointing to MeetingManager app
    - Configure test bundle to import @testable import MeetingManager
    - Add XCTest framework dependency
    - Create MeetingManagerTests/MeetingManagerTests.swift as placeholder with basic XCTestCase

    If test target already exists, verify configuration:
    - Check test host is set correctly
    - Verify import access with @testable
    - Ensure all source files are accessible to tests

    Create test file structure:
    - MeetingManagerTests/Models/ (for model tests)
    - MeetingManagerTests/Services/ (for service tests)
    - MeetingManagerTests/Utilities/ (for utility tests)

    Why: Proper test infrastructure is prerequisite for all test development
  </action>
  <verify>Run xcodebuild test -scheme MeetingManager -destination 'platform=iOS Simulator,name=iPhone 15' - should complete without target errors</verify>
  <done>Test target exists, builds successfully, basic test runs and passes</done>
</task>

<task type="auto">
  <name>Task 2: Write unit tests for NameMatcher utility</name>
  <files>MeetingManagerTests/Utilities/NameMatcherTests.swift</files>
  <action>
    Create comprehensive test suite for NameMatcher.findBestMatch:

    Test cases:
    - Exact match: "John Doe" matches ["John Doe", "Jane Doe"] → "John Doe"
    - Case insensitive: "john doe" matches ["John Doe"] → "John Doe"
    - First name only: "John" matches ["John Doe", "John Smith"] → first match
    - Last name only: "Doe" matches ["John Doe", "Jane Doe"] → first match
    - Partial match: "J. Doe" matches ["John Doe"] → "John Doe"
    - Multiple spaces: "John  Doe" (double space) matches ["John Doe"] → "John Doe"
    - Leading/trailing whitespace: " John Doe " matches ["John Doe"] → "John Doe"
    - No match: "Bob" matches ["John Doe"] → nil
    - Empty input: "" matches ["John Doe"] → nil
    - Empty members array: "John" matches [] → nil
    - Unicode names: "José García" matches ["Jose Garcia", "José García"] → prefer exact
    - Hyphenated names: "Mary-Jane" matches ["Mary Jane", "MaryJane"] → fuzzy match

    Use XCTest assertions:
    - XCTAssertEqual for positive matches
    - XCTAssertNil for no-match cases
    - Descriptive test method names: testExactMatchReturnsCorrectMember()

    Why: NameMatcher is critical for task assignment accuracy; comprehensive tests prevent assignee errors
  </action>
  <verify>Run xcodebuild test -scheme MeetingManager -only-testing:MeetingManagerTests/NameMatcherTests - all tests pass</verify>
  <done>12+ test cases covering NameMatcher edge cases, all passing</done>
</task>

<task type="auto">
  <name>Task 3: Write unit tests for Meeting model and ProcessingState</name>
  <files>MeetingManagerTests/Models/MeetingTests.swift, MeetingManagerTests/Models/ProcessingStateTests.swift</files>
  <action>
    Create MeetingTests.swift testing Meeting business logic:

    Test Meeting state properties:
    - isActive: true when startedAt != nil && endedAt == nil
    - isActive: false when not started
    - isActive: false when ended
    - isUpcoming: true when scheduledAt > now && startedAt == nil
    - isPast: true when endedAt != nil || (scheduledAt < now && not started)

    Test Meeting initialization:
    - Codable encoding/decoding matches database format
    - UUID fields serialize correctly
    - Date fields use ISO8601 format
    - Optional fields (documentText, summary) encode as null when nil

    Create ProcessingStateTests.swift:

    Test ProcessingState enum:
    - isProcessing returns true for uploading/parsing/generating/extracting
    - isProcessing returns false for idle/completed/failed
    - shouldShowProgress returns true for states with progress indicators
    - estimatedDuration returns correct values (parsing: 5s, generating: 10s)
    - displayMessage returns user-friendly strings

    Why: State logic drives UI behavior; incorrect state can confuse users or hide critical info
  </action>
  <verify>Run xcodebuild test -scheme MeetingManager -only-testing:MeetingManagerTests/MeetingTests - all tests pass</verify>
  <done>15+ test cases covering Meeting state transitions and ProcessingState behavior, all passing</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Test target builds successfully
- [ ] All test files compile without errors
- [ ] xcodebuild test -scheme MeetingManager passes all tests
- [ ] Test coverage includes NameMatcher, Meeting state, ProcessingState
- [ ] No flaky tests (run suite 3 times to verify)
</verification>

<success_criteria>

- Test target exists and builds successfully
- 25+ unit tests written covering critical utilities and models
- All tests pass consistently
- NameMatcher has comprehensive edge case coverage
- Meeting state logic validated with tests
- ProcessingState enum behavior verified
  </success_criteria>

<output>
After completion, create `.planning/phases/07-polish-testing/07-02-SUMMARY.md`
</output>
