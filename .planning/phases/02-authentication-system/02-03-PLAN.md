---
phase: 02-authentication-system
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: [MeetingManager/Views/Auth/LoginView.swift, MeetingManager/Views/Auth/SignupView.swift, MeetingManager/Views/Auth/AuthContainerView.swift]
autonomous: false
---

<objective>
Create SwiftUI authentication screens for login and signup.

Purpose: User-facing UI for email/password authentication
Output: LoginView, SignupView, and AuthContainerView with form validation and error handling
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-system/02-RESEARCH.md
@.planning/phases/02-authentication-system/02-01-SUMMARY.md

AuthService from plan 02-01:
@MeetingManager/Auth/AuthService.swift

Navigation architecture from Phase 1:
@MeetingManager/Navigation/Route.swift
@MeetingManager/Navigation/AppCoordinator.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LoginView with form validation</name>
  <files>MeetingManager/Views/Auth/LoginView.swift</files>
  <action>
Create LoginView.swift following research pattern (see 02-RESEARCH.md "Login View with Error Handling"):

```swift
import SwiftUI

struct LoginView: View {
    @State private var email = ""
    @State private var password = ""
    @State private var errorMessage: String?
    @State private var isLoading = false

    let authService: AuthService
    let onLoginSuccess: () -> Void

    var body: some View {
        VStack(spacing: 20) {
            Text("Welcome Back")
                .font(.largeTitle)
                .fontWeight(.bold)

            TextField("Email", text: $email)
                .textContentType(.emailAddress)
                .textInputAutocapitalization(.never)
                .keyboardType(.emailAddress)
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(8)

            SecureField("Password", text: $password)
                .textContentType(.password)
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(8)

            if let errorMessage {
                Text(errorMessage)
                    .foregroundStyle(.red)
                    .font(.caption)
            }

            Button {
                Task {
                    await signIn()
                }
            } label: {
                if isLoading {
                    ProgressView()
                        .progressViewStyle(CircularProgressViewStyle(tint: .white))
                } else {
                    Text("Sign In")
                        .frame(maxWidth: .infinity)
                }
            }
            .disabled(isLoading || !isFormValid)
            .frame(maxWidth: .infinity)
            .padding()
            .background(isFormValid ? Color.blue : Color.gray)
            .foregroundColor(.white)
            .cornerRadius(8)
        }
        .padding()
    }

    private var isFormValid: Bool {
        !email.isEmpty && email.contains("@") && password.count >= 6
    }

    private func signIn() async {
        isLoading = true
        errorMessage = nil

        do {
            _ = try await authService.signIn(email: email, password: password)
            await MainActor.run {
                onLoginSuccess()
            }
        } catch {
            await MainActor.run {
                errorMessage = error.localizedDescription
                isLoading = false
            }
        }
    }
}
```

Key features:
- Email validation (must contain @)
- Password minimum length (6 characters)
- Loading state with ProgressView
- Error message display
- Disabled button when form invalid
- Uses AuthService from plan 02-01

Why async/await: Modern Swift concurrency for auth operations (see research).
  </action>
  <verify>Build succeeds, LoginView renders in preview without errors</verify>
  <done>LoginView complete with validation and error handling</done>
</task>

<task type="auto">
  <name>Task 2: Create SignupView with password confirmation</name>
  <files>MeetingManager/Views/Auth/SignupView.swift</files>
  <action>
Create SignupView.swift similar to LoginView but with additional fields:

```swift
import SwiftUI

struct SignupView: View {
    @State private var email = ""
    @State private var password = ""
    @State private var confirmPassword = ""
    @State private var errorMessage: String?
    @State private var isLoading = false

    let authService: AuthService
    let onSignupSuccess: () -> Void

    var body: some View {
        VStack(spacing: 20) {
            Text("Create Account")
                .font(.largeTitle)
                .fontWeight(.bold)

            TextField("Email", text: $email)
                .textContentType(.emailAddress)
                .textInputAutocapitalization(.never)
                .keyboardType(.emailAddress)
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(8)

            SecureField("Password", text: $password)
                .textContentType(.newPassword)
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(8)

            SecureField("Confirm Password", text: $confirmPassword)
                .textContentType(.newPassword)
                .padding()
                .background(Color(.systemGray6))
                .cornerRadius(8)

            if let errorMessage {
                Text(errorMessage)
                    .foregroundStyle(.red)
                    .font(.caption)
            }

            // Password requirements hint
            if !password.isEmpty && password.count < 8 {
                Text("Password must be at least 8 characters")
                    .foregroundStyle(.orange)
                    .font(.caption)
            }

            Button {
                Task {
                    await signUp()
                }
            } label: {
                if isLoading {
                    ProgressView()
                        .progressViewStyle(CircularProgressViewStyle(tint: .white))
                } else {
                    Text("Sign Up")
                        .frame(maxWidth: .infinity)
                }
            }
            .disabled(isLoading || !isFormValid)
            .frame(maxWidth: .infinity)
            .padding()
            .background(isFormValid ? Color.blue : Color.gray)
            .foregroundColor(.white)
            .cornerRadius(8)
        }
        .padding()
    }

    private var isFormValid: Bool {
        email.contains("@") &&
        password.count >= 8 &&
        password == confirmPassword
    }

    private func signUp() async {
        isLoading = true
        errorMessage = nil

        do {
            _ = try await authService.signUp(email: email, password: password)
            await MainActor.run {
                onSignupSuccess()
            }
        } catch {
            await MainActor.run {
                errorMessage = error.localizedDescription
                isLoading = false
            }
        }
    }
}
```

Validation rules (from research "Pitfall 5: Weak Password Validation"):
- Email must contain @
- Password minimum 8 characters (stronger than 6)
- Password must match confirmation
- Helpful error messages

Do NOT implement complex regex or password strength checking on client - Supabase handles server-side validation.
  </action>
  <verify>Build succeeds, SignupView renders in preview without errors</verify>
  <done>SignupView complete with password confirmation and validation</done>
</task>

<task type="auto">
  <name>Task 3: Create AuthContainerView with tab switching</name>
  <files>MeetingManager/Views/Auth/AuthContainerView.swift</files>
  <action>
Create AuthContainerView that switches between LoginView and SignupView:

```swift
import SwiftUI

struct AuthContainerView: View {
    @State private var selectedTab = 0
    let authService: AuthService
    let onAuthSuccess: () -> Void

    var body: some View {
        VStack {
            // Tab picker
            Picker("Auth Type", selection: $selectedTab) {
                Text("Sign In").tag(0)
                Text("Sign Up").tag(1)
            }
            .pickerStyle(.segmented)
            .padding()

            // Content
            TabView(selection: $selectedTab) {
                LoginView(
                    authService: authService,
                    onLoginSuccess: onAuthSuccess
                )
                .tag(0)

                SignupView(
                    authService: authService,
                    onSignupSuccess: onAuthSuccess
                )
                .tag(1)
            }
            .tabViewStyle(.page(indexDisplayMode: .never))
        }
    }
}

#Preview {
    AuthContainerView(
        authService: AuthService(),
        onAuthSuccess: {}
    )
}
```

Simple tab-based UI:
- Segmented control for tab selection
- TabView with page style
- Passes authService and onAuthSuccess to both views
  </action>
  <verify>Build succeeds, AuthContainerView renders with tab switching working</verify>
  <done>AuthContainerView complete with login/signup switching</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Authentication UI screens with form validation</what-built>
  <how-to-verify>
    1. Open Xcode and run the app in iOS Simulator (⌘R)
    2. You should see AuthContainerView as the initial screen
    3. Test Login tab:
       - Type invalid email (no @) - button should be disabled
       - Type valid email and short password (<6 chars) - button disabled
       - Type valid email and 6+ char password - button enabled
       - Verify error message appears if login fails
    4. Test Signup tab:
       - Type valid email and password (8+ chars)
       - Type different confirm password - button disabled
       - Type matching confirm password - button enabled
       - Verify password hint appears when password <8 chars
    5. Check visual polish:
       - Forms are properly spaced
       - Buttons are full width
       - Loading spinner shows during auth
       - Error messages are visible and red
  </how-to-verify>
  <resume-signal>Type "approved" to continue, or describe UI/validation issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] swift build succeeds
- [ ] All three views render without errors
- [ ] Form validation works correctly
- [ ] User verification passed
</verification>

<success_criteria>
- All tasks completed
- LoginView and SignupView follow research patterns
- Form validation prevents invalid submissions
- Error handling displays user-friendly messages
- User approved visual design and validation logic
- Ready for state management integration (plan 02-04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication-system/02-03-SUMMARY.md`
</output>
