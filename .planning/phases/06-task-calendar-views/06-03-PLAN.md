---
phase: 06-task-calendar-views
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: [MeetingManager/Views/Organization/OrganizationDetailView.swift, MeetingManager/Views/Task/TaskListView.swift, MeetingManager/Views/Calendar/CalendarView.swift]
autonomous: true
---

<objective>
Polish task and calendar views with enhanced UX: section navigation from OrganizationDetailView, loading states, error handling, and visual refinements.

Purpose: Ensure task and calendar features are production-ready with smooth interactions, clear feedback, and professional appearance.
Output: Polished task and calendar views integrated seamlessly into organization workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-task-calendar-views/06-01-PLAN.md
@.planning/phases/06-task-calendar-views/06-02-PLAN.md
@MeetingManager/MeetingManager/Views/Organization/OrganizationDetailView.swift
@MeetingManager/MeetingManager/State/MeetingState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance OrganizationDetailView with Tasks and Calendar section</name>
  <files>MeetingManager/MeetingManager/Views/Organization/OrganizationDetailView.swift</files>
  <action>
Add Tasks and Calendar navigation section to OrganizationDetailView:

**Section placement:**
- After meetings section, before members section
- Section header: "Planning" or "Organize"

**Tasks row:**
- NavigationLink to TaskListView
- Leading: Green checkmark.circle.fill icon (24pt)
- Title: "Tasks"
- Trailing: Badge showing incomplete task count (if > 0)
- Subtitle: "\(incompleteCount) active" in gray caption font

**Calendar row:**
- NavigationLink to CalendarView
- Leading: Blue calendar icon (24pt)
- Title: "Calendar"
- Subtitle: "Meetings & deadlines" in gray caption font

**Task count computation:**
- Add computed property: `incompleteTaskCount`
- Filter meetingState.organizationTasks where !isCompleted
- Return count
- Updates automatically when organizationTasks changes

**Preload tasks for badge:**
- In .task modifier (existing), also call:
  - `await meetingState.loadOrganizationTasks(organizationId: organization.id)`
- Handle errors silently for badge (don't block view)
- Badge shows "0" if loading fails

**Visual design:**
- LabeledContent for consistent layout
- Divider between rows
- Background color: .secondarySystemGroupedBackground
- Corner radius: 12pt
- Padding: 16pt

**Why this approach:**
- Grouped section provides clear organization boundaries
- Badge provides at-a-glance task status
- Preloading tasks enables immediate badge display
- Follows iOS design patterns (Settings app style)
- Subtitle text clarifies purpose of each view
  </action>
  <verify>
Build succeeds:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>OrganizationDetailView has Planning section with Tasks and Calendar navigation, task count badge displays correctly</done>
</task>

<task type="auto">
  <name>Task 2: Add loading states and error handling to TaskListView</name>
  <files>MeetingManager/MeetingManager/Views/Task/TaskListView.swift</files>
  <action>
Enhance TaskListView with comprehensive loading and error handling:

**Loading state:**
- Show ProgressView() overlay when meetingState.isLoadingTasks == true
- Centered with "Loading tasks..." label
- Translucent background

**Error handling:**
- @State var errorMessage: String? = nil
- @State var showError: Bool = false
- Wrap loadOrganizationTasks in do-catch
- On error: set errorMessage and showError = true
- .alert("Error", isPresented: $showError) with error message

**Empty states:**
- When tasks.isEmpty && !isLoadingTasks:
  - ContentUnavailableView (iOS 17+)
  - Icon: checkmark.circle (large, gray)
  - Title: "No Tasks Yet"
  - Description: "Tasks will appear here when extracted from meeting documents"

**Pull to refresh:**
- .refreshable { await loadTasks() }
- Helper method loadTasks() that handles error gracefully
- Shows error alert if refresh fails

**Task toggle error handling:**
- Wrap toggleTaskCompletion in do-catch
- On error: show alert with error message
- Revert UI change on error (task stays in original state)
- Optimistic UI update before API call, rollback on failure

**Why this approach:**
- Loading states prevent user confusion during data fetch
- Error alerts provide actionable feedback
- Empty state guides users on feature purpose
- Optimistic UI provides instant feedback
- Pull-to-refresh follows iOS conventions
  </action>
  <verify>
Build succeeds:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>TaskListView has loading spinner, error handling, empty states, and pull-to-refresh</done>
</task>

<task type="auto">
  <name>Task 3: Add loading states and visual polish to CalendarView</name>
  <files>MeetingManager/MeetingManager/Views/Calendar/CalendarView.swift</files>
  <action>
Enhance CalendarView with loading states and visual refinements:

**Loading state:**
- Show ProgressView() overlay during initial load
- When meetingState.isLoadingMeetings || meetingState.isLoadingTasks
- "Loading calendar..." label
- Translucent background

**Error handling:**
- @State var errorMessage: String? = nil
- @State var showError: Bool = false
- Wrap data loading in do-catch blocks
- Display .alert for errors

**Pull to refresh:**
- .refreshable { await reloadCalendarData() }
- Reloads both meetings and tasks
- Shows errors via alert if either fails

**Visual enhancements:**
- Month header: Larger font (title2), bold
- Arrow buttons: Larger tap targets (44x44pt minimum)
- Day cells:
  - Subtle shadow on selected day
  - Smooth animation on selection change
  - Increased spacing between cells (2pt)
- Indicator dots:
  - Blue: meetings (Circle, 6pt diameter)
  - Green: tasks (Circle, 6pt diameter)
  - HStack when both present, 2pt spacing
- Today indicator: Circle stroke (2pt width, blue color)

**Empty calendar state:**
- When no meetings and no tasks for entire month:
  - Show message in day detail: "No events this month"
  - Suggest: "Schedule a meeting or create tasks to get started"

**Month boundaries:**
- Disable previous month button before organization creation date
- Grey out dates before today (past dates)
- Different opacity for adjacent month dates (0.4)

**Animation:**
- .animation(.easeInOut(duration: 0.2), value: selectedDate)
- Smooth transitions when selecting dates
- Smooth month changes

**Why this approach:**
- Loading states prevent blank screen confusion
- Visual polish improves perceived quality
- Animations make interactions feel responsive
- Month boundaries prevent navigation to irrelevant dates
- Empty states guide user behavior
  </action>
  <verify>
Build succeeds:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>CalendarView has loading states, error handling, visual polish, and smooth animations</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build` succeeds
- [ ] OrganizationDetailView shows Planning section with task count badge
- [ ] TaskListView has loading spinner and error handling
- [ ] CalendarView has visual polish and animations
- [ ] All views handle empty states gracefully
- [ ] No build errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Navigation section integrated in OrganizationDetailView
- Loading and error states implemented
- Visual polish applied to calendar
- Empty states provide user guidance
- No build errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-calendar-views/06-03-SUMMARY.md`
</output>
