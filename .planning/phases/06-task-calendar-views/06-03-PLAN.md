---
phase: 06-task-calendar-views
plan: 03
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified: [MeetingManager/Views/RootView.swift, MeetingManager/Views/MainTabView.swift, MeetingManager/Views/Profile/ProfileView.swift, MeetingManager/State/MeetingState.swift]
autonomous: true
---

<objective>
Implement bottom tab bar navigation with four tabs: Organizations, Calendar, Tasks, and Profile.

Purpose: Provide easy access to main app features via bottom tab bar, replacing the nested navigation structure.
Output: Working TabView with all four tabs integrated and data loading properly.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-task-calendar-views/06-01-PLAN.md
@.planning/phases/06-task-calendar-views/06-02-PLAN.md
@MeetingManager/MeetingManager/Views/RootView.swift
@MeetingManager/MeetingManager/State/MeetingState.swift
@MeetingManager/MeetingManager/State/OrganizationState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MainTabView with four-tab bottom navigation</name>
  <files>MeetingManager/MeetingManager/Views/MainTabView.swift</files>
  <action>
Create MainTabView that wraps all main app views in a TabView:

**Structure:**
- TabView with four tabs
- Each tab is a separate NavigationStack (for independent navigation hierarchies)
- Tab selection state managed by @State

**Tab 1 - Organizations:**
- Content: OrganizationListView (existing from Phase 3)
- Icon: person.3.fill
- Label: "Organizations"
- Badge: None

**Tab 2 - Calendar:**
- Content: CalendarView (from 06-02)
- Icon: calendar
- Label: "Calendar"
- Badge: None
- Loads ALL meetings and tasks from ALL organizations

**Tab 3 - Tasks:**
- Content: TaskListView (from 06-01)
- Icon: checkmark.circle.fill
- Label: "Tasks"
- Badge: Incomplete task count (if > 0)
- Loads ALL tasks from ALL organizations

**Tab 4 - Profile:**
- Content: ProfileView (new, basic implementation)
- Icon: person.fill
- Label: "Profile"
- Badge: None

**State injection:**
- @Environment(MeetingState.self)
- @Environment(OrganizationState.self)
- @State private var authState: AuthState
- All environment objects passed to each tab's content

**Data preloading:**
- Load organizations on appear (already exists in OrganizationListView)
- Calendar and Tasks tabs load their data when first selected
- Use .task modifier on each tab's root view

**Badge calculation:**
- For Tasks tab: count of incomplete tasks from meetingState.allUserTasks
- Update dynamically when tasks change
- Use @State to track badge count

**Why this approach:**
- Each tab has independent NavigationStack for proper navigation
- TabView is native iOS pattern users expect
- Badges provide at-a-glance status
- State injection ensures all tabs have access to data
- Preloading on first appearance prevents blank screens
  </action>
  <verify>
Build succeeds:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>MainTabView displays with all four tabs, each with proper icon and label, navigation works within each tab</done>
</task>

<task type="auto">
  <name>Task 2: Create basic ProfileView and add methods to load all meetings/tasks</name>
  <files>MeetingManager/MeetingManager/Views/Profile/ProfileView.swift, MeetingManager/MeetingManager/State/MeetingState.swift</files>
  <action>
Create ProfileView and extend MeetingState with methods to load all data:

**ProfileView structure:**
- NavigationStack with "Profile" title
- User email display (from authState.currentUser)
- "Account" section with email in LabeledContent
- "Actions" section with:
  - "Sign Out" button (red, calls authState.signOut())
- Simple, clean design using iOS standard components

**ProfileView state:**
- @Environment(\.authState) for user data and sign out
- @State for showing sign out confirmation alert

**MeetingState additions:**

1. `loadAllMeetings(organizationIds: [UUID]) async throws`:
   - Similar to loadAllUserTasks from 06-01
   - For each organizationId, call MeetingService().fetchMeetingsForOrganization(organizationId)
   - Aggregate all results into allMeetings array
   - Add property: `allMeetings: [Meeting] = []`
   - Set isLoadingMeetings during fetch
   - @MainActor annotation

2. Update `isLoadingMeetings` property:
   - Make it public (not private(set))
   - Or add separate `isLoadingAllMeetings` property

**Why this approach:**
- ProfileView is minimal v1 - just sign out functionality
- loadAllMeetings enables CalendarView to show all events
- Matches pattern from loadAllUserTasks
- Keeps state management centralized in MeetingState
  </action>
  <verify>
Build succeeds:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>ProfileView displays user info and sign out button, MeetingState has loadAllMeetings method</done>
</task>

<task type="auto">
  <name>Task 3: Replace OrganizationListView in RootView with MainTabView</name>
  <files>MeetingManager/MeetingManager/Views/RootView.swift, MeetingManager/MeetingManager.xcodeproj/project.pbxproj</files>
  <action>
Update RootView to use MainTabView instead of directly showing OrganizationListView:

**RootView changes:**
- Replace the authenticated branch from:
  ```swift
  OrganizationListView()
      .environment(authState)
      .environment(organizationState)
      .environment(meetingState)
  ```
- To:
  ```swift
  MainTabView()
      .environment(authState)
      .environment(organizationState)
      .environment(meetingState)
  ```

**Xcode project update:**
- Add MainTabView.swift to Views/ directory
- Add ProfileView.swift to Views/Profile/ directory
- Ensure both files added to MeetingManager target
- Use xcodeproj gem or manual Xcode addition

**Remove old navigation:**
- OrganizationDetailView no longer needs Tasks/Calendar section
- Remove the Planning section that was added in previous implementation
- Keep OrganizationDetailView as detail view accessible from Organizations tab
- Meeting navigation remains unchanged

**Data flow:**
- Organizations tab: Loads organizations on appear (existing)
- Calendar tab: Calls meetingState.loadAllMeetings and loadAllUserTasks on appear
- Tasks tab: Calls meetingState.loadAllUserTasks on appear
- Profile tab: No data loading needed

**Why this approach:**
- Replaces root navigation structure with tab-based navigation
- Maintains all existing views, just changes how they're accessed
- Each tab independently manages its data loading
- Environment injection passes state to all tabs
- Simpler navigation hierarchy (no deep nesting)
  </action>
  <verify>
Build succeeds and tab bar is visible:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>RootView uses MainTabView, tab bar displays with all four tabs, switching between tabs works, data loads properly</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build` succeeds
- [ ] MainTabView displays with four tabs at bottom
- [ ] Organizations, Calendar, Tasks, and Profile tabs all functional
- [ ] Calendar shows events from all organizations
- [ ] Tasks shows tasks from all organizations
- [ ] Tab switching works smoothly
- [ ] No build errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Bottom tab bar navigation working with all four tabs
- Multi-organization data aggregation functioning
- Profile view with sign out capability
- No build errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-calendar-views/06-03-SUMMARY.md`
</output>
