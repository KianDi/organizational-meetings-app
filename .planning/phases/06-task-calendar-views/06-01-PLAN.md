---
phase: 06-task-calendar-views
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [MeetingManager/Views/Task/TaskListView.swift, MeetingManager/State/MeetingState.swift]
autonomous: true
---

<objective>
Create comprehensive task list view showing all organization tasks with filtering, sorting, and completion tracking.

Purpose: Provide members with a centralized view of all their tasks across meetings, with ability to filter by assignee and mark tasks complete.
Output: Working TaskListView integrated with MeetingState for task management.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-document-upload-ai-processing/05-04-SUMMARY.md
@.planning/phases/05-document-upload-ai-processing/05-05-SUMMARY.md
@MeetingManager/MeetingManager/Models/MeetingTask.swift
@MeetingManager/MeetingManager/Services/TaskService.swift
@MeetingManager/MeetingManager/Views/Meeting/TaskRowView.swift
@MeetingManager/MeetingManager/State/MeetingState.swift
@MeetingManager/MeetingManager/State/OrganizationState.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Task directory and TaskListView with filtering and sorting</name>
  <files>MeetingManager/MeetingManager/Views/Task/TaskListView.swift</files>
  <action>
Create TaskListView that displays all tasks for an organization with comprehensive filtering and sorting:

**Structure:**
- NavigationStack with "Tasks" title
- Toolbar with filter menu (All Tasks / My Tasks / Unassigned)
- Segmented picker for completion filter (All / Active / Completed)
- List grouped by sections: Overdue, Today, This Week, Later, Completed
- Each row uses TaskRowView component from Phase 5
- Empty state with "No tasks yet" message and icon
- Pull-to-refresh to reload tasks

**State management:**
- @Environment(MeetingState.self) for task data
- @Environment(OrganizationState.self) for current org and member names
- @Environment(\.authState) for current user ID
- @State for filter selections (assigneeFilter, completionFilter)
- Computed property for filtered tasks based on selections

**Filtering logic:**
- Assignee filter: All (no filter), My Tasks (assigneeId == currentUserId), Unassigned (assigneeId == nil)
- Completion filter: All (no filter), Active (!isCompleted), Completed (isCompleted)

**Sorting/grouping:**
- Overdue: !isCompleted && dueDate < today
- Today: !isCompleted && dueDate == today
- This Week: !isCompleted && dueDate <= 7 days from now
- Later: !isCompleted && (dueDate > 7 days OR dueDate == nil)
- Completed: isCompleted (sorted by completion date desc if available, otherwise by due date)

**Row interaction:**
- Tap checkbox to toggle completion (calls MeetingState.toggleTaskCompletion)
- Tap row to navigate to meeting detail where task was created
- Swipe to delete (admin/creator only)

**Why this approach:**
- Reuses TaskRowView for consistency with meeting detail view
- Groups provide clear organization and prioritization
- Filters enable personalization (show only my tasks)
- Pull-to-refresh follows iOS conventions
- Navigation to meeting provides context for each task

Do NOT create custom task detail view - tasks are best viewed in their meeting context.
  </action>
  <verify>
Build succeeds with no errors:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>TaskListView displays tasks grouped by date sections, filtering works for assignee and completion status, task rows are tappable</done>
</task>

<task type="auto">
  <name>Task 2: Add task management methods to MeetingState</name>
  <files>MeetingManager/MeetingManager/State/MeetingState.swift</files>
  <action>
Extend MeetingState with task-specific methods:

**Add properties:**
- `organizationTasks: [MeetingTask] = []` - cached tasks for current organization
- `isLoadingTasks: Bool = false` - loading indicator

**Add methods:**

1. `loadOrganizationTasks(organizationId: UUID) async throws`:
   - Set isLoadingTasks = true
   - Call TaskService().fetchTasksForOrganization(organizationId)
   - Update organizationTasks with results
   - Set isLoadingTasks = false
   - Handle errors with isLoadingTasks = false in defer block

2. `toggleTaskCompletion(taskId: UUID) async throws`:
   - Find task in organizationTasks by id
   - Call TaskService().updateTaskCompletion(taskId, isCompleted: !currentValue)
   - Update local task in organizationTasks array with new completion status
   - If task also exists in a meeting's tasks array, update it there too
   - @MainActor annotation for UI updates

3. `deleteTask(taskId: UUID) async throws`:
   - Call TaskService().deleteTask(taskId)
   - Remove from organizationTasks array
   - Remove from any meeting's tasks array
   - @MainActor annotation

**Why this approach:**
- Centralizes task state management in existing MeetingState
- Follows established pattern from loadMeetingWithTasks
- Local array updates provide instant UI feedback
- Dual cache update (organizationTasks + meeting.tasks) prevents stale data
- Actor isolation ensures thread safety
  </action>
  <verify>
Build succeeds and new methods are accessible:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>MeetingState has loadOrganizationTasks, toggleTaskCompletion, and deleteTask methods with proper error handling</done>
</task>

<task type="auto">
  <name>Task 3: Update Xcode project and integrate TaskListView into navigation</name>
  <files>MeetingManager/MeetingManager.xcodeproj/project.pbxproj, MeetingManager/MeetingManager/Views/Organization/OrganizationDetailView.swift</files>
  <action>
Add TaskListView to Xcode project and integrate into app navigation:

**Xcode project update:**
- Add TaskListView.swift to Xcode project in Views/Task group
- Use `open -a Xcode MeetingManager/MeetingManager.xcodeproj` then manually add file to project
- Ensure file is added to MeetingManager target

**Navigation integration in OrganizationDetailView:**
- Add "Tasks" button/row in organization detail view
- NavigationLink to TaskListView passing organizationId
- Icon: checkmark.circle.fill (green, size 24)
- Badge showing incomplete task count (if > 0)
- Position: Below meetings section, above members section

**Initial load:**
- TaskListView .task { await meetingState.loadOrganizationTasks(organizationId) }
- Shows loading indicator while fetching
- Handles errors with alert presentation

**Why this approach:**
- Tasks accessible from organization detail (central hub)
- Badge provides at-a-glance incomplete task count
- Follows existing navigation patterns (like MeetingListView)
- .task modifier ensures data loads on appear
  </action>
  <verify>
Build succeeds and TaskListView is navigable from OrganizationDetailView:
xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build
  </verify>
  <done>TaskListView added to Xcode project, accessible via NavigationLink from OrganizationDetailView, loads tasks on appear</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project MeetingManager/MeetingManager.xcodeproj -scheme MeetingManager -sdk iphonesimulator clean build` succeeds
- [ ] TaskListView file exists in Views/Task/
- [ ] MeetingState has new task management methods
- [ ] Navigation from OrganizationDetailView to TaskListView works
- [ ] No TypeScript/Swift errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- TaskListView displays tasks with filtering and grouping
- Task completion toggle works
- Navigation integrated
- No build errors or warnings
</success_criteria>

<output>
After completion, create `.planning/phases/06-task-calendar-views/06-01-SUMMARY.md`
</output>
