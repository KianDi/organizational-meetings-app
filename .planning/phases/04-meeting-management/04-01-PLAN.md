---
phase: 04-meeting-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [MeetingManager/Database/schema.sql, MeetingManager/Services/MeetingService.swift, MeetingManager/Views/Meeting/CreateMeetingView.swift, MeetingManager.xcodeproj/project.pbxproj]
autonomous: true
---

<objective>
Create meeting database schema and service layer for organizers to schedule meetings.

Purpose: Enable organizers to create meetings with title and scheduled time, establishing the foundation for meeting management.
Output: PostgreSQL meetings table with RLS policies, MeetingService actor for CRUD operations, CreateMeetingView UI for meeting scheduling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@MeetingManager/MeetingManager/Models/Meeting.swift
@MeetingManager/MeetingManager/Models/Organization.swift
@MeetingManager/MeetingManager/Services/OrganizationService.swift
@MeetingManager/MeetingManager/Database/schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create meetings table schema with RLS policies</name>
  <files>MeetingManager/Database/schema.sql</files>
  <action>Append to existing schema.sql file. Create meetings table matching Meeting model structure: id (UUID PK), organization_id (UUID FK to organizations), title (TEXT), scheduled_at (TIMESTAMPTZ), started_at (TIMESTAMPTZ NULL), ended_at (TIMESTAMPTZ NULL), google_docs_url (TEXT NULL), summary (TEXT NULL), attendee_ids (UUID[]), created_by_id (UUID FK to auth.users). Enable RLS. Add policies: (1) Members can SELECT meetings for their organizations (use organizations.member_ids), (2) Admins can INSERT/UPDATE/DELETE meetings for their organizations (check admin_id match), (3) Members can UPDATE attendee_ids only (for check-in). Create GIN index on attendee_ids, B-tree indexes on organization_id, created_by_id, scheduled_at for efficient queries. Follow exact pattern from organizations table.</action>
  <verify>schema.sql contains meetings table definition with 4 RLS policies and 4 indexes matching Meeting model exactly</verify>
  <done>Database schema ready for Supabase SQL Editor execution</done>
</task>

<task type="auto">
  <name>Task 2: Create MeetingService actor with CRUD operations</name>
  <files>MeetingManager/Services/MeetingService.swift</files>
  <action>Create MeetingService actor following OrganizationService pattern. Private MeetingDTO struct with CodingKeys for snake_case mapping (organization_id, scheduled_at, started_at, ended_at, google_docs_url, attendee_ids, created_by_id). Init with SupabaseClient dependency injection. Methods: createMeeting(organizationId, title, scheduledAt, createdById) -> Meeting, fetchMeetingsForOrganization(organizationId) -> [Meeting] ordered by scheduledAt descending, fetchMeeting(id) -> Meeting, updateMeeting(id, updates) for started_at/ended_at changes. Use async/await, handle Supabase errors with throw, convert MeetingDTO to Meeting model. Add XML documentation comments for all public methods. Follow actor isolation rules for thread safety.</action>
  <verify>swift build succeeds, MeetingService compiles with no warnings, all methods return/throw correctly</verify>
  <done>MeetingService ready for use in views with createMeeting and fetchMeetingsForOrganization working</done>
</task>

<task type="auto">
  <name>Task 3: Create CreateMeetingView for organizers</name>
  <files>MeetingManager/Views/Meeting/CreateMeetingView.swift, MeetingManager.xcodeproj/project.pbxproj</files>
  <action>Create CreateMeetingView in new Views/Meeting directory. Form with TextField for title (validation: not empty), DatePicker for scheduledAt (minimum: now, display: date and time, default: 1 hour from now). @State for title, scheduledAt, isSubmitting, errorMessage. Async submit button calls MeetingService.createMeeting with organization.id and authState.currentUser.id. Show loading spinner during submission. Display error below form if creation fails. Dismiss on success via @Environment(\.dismiss). NavigationStack with toolbar Cancel/Done buttons (Done disabled when title empty or isSubmitting). Follow exact CreateOrganizationView pattern from Phase 3. Add MeetingManager/Views/Meeting group to Xcode project.</action>
  <verify>xcodebuild -project MeetingManager.xcodeproj -scheme MeetingManager build succeeds, CreateMeetingView renders in preview</verify>
  <done>Organizers can create meetings with title and scheduled time, validation prevents empty titles, form dismisses on success</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] schema.sql contains meetings table with RLS policies matching Meeting model
- [ ] MeetingService compiles and provides createMeeting + fetchMeetingsForOrganization
- [ ] CreateMeetingView builds and validates input correctly
- [ ] No compiler errors or warnings
</verification>

<success_criteria>

- All tasks completed
- Meetings table schema ready for Supabase
- MeetingService actor follows established patterns
- CreateMeetingView functional with validation
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/04-meeting-management/04-01-SUMMARY.md`
</output>
