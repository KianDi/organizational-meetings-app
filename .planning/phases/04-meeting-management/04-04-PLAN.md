---
phase: 04-meeting-management
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified: [MeetingManager/State/MeetingState.swift, MeetingManager/Views/RootView.swift, MeetingManager/Views/Meeting/MeetingListView.swift, MeetingManager/Views/Meeting/MeetingDetailView.swift, MeetingManager.xcodeproj/project.pbxproj]
autonomous: true
---

<objective>
Create MeetingState for app-wide meeting management and integrate with existing views.

Purpose: Centralize meeting state management following the OrganizationState pattern, enabling reactive updates across the app when meetings are created, started, ended, or checked into.
Output: MeetingState class with @Observable, integration into RootView, wrapper components for state-aware meeting operations.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meeting-management/04-02-SUMMARY.md
@.planning/phases/04-meeting-management/04-03-SUMMARY.md
@MeetingManager/MeetingManager/State/OrganizationState.swift
@MeetingManager/MeetingManager/Services/MeetingService.swift
@MeetingManager/MeetingManager/Views/RootView.swift
@MeetingManager/MeetingManager/Views/Meeting/MeetingListView.swift
@MeetingManager/MeetingManager/Views/Meeting/MeetingDetailView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MeetingState with @Observable pattern</name>
  <files>MeetingManager/State/MeetingState.swift, MeetingManager.xcodeproj/project.pbxproj</files>
  <action>Create MeetingState class following OrganizationState pattern exactly. @Observable final class with private(set) var meetings: [Meeting] = [], isLoading: Bool = false. Private let meetingService: MeetingService with default init. @MainActor methods: loadMeetings(organizationId: UUID) async throws (fetches and stores meetings), createMeeting(organizationId, title, scheduledAt, createdById) async throws -> Meeting (creates and appends to meetings array), checkIn(meetingId, userId) async throws (calls service, updates meeting in array), startMeeting(meetingId) async throws (updates started_at, refreshes array), endMeeting(meetingId) async throws (updates ended_at, refreshes array). Each mutating method updates local meetings array after successful service call for instant UI updates. Use defer for isLoading cleanup. Match thread-safety pattern from OrganizationState with @MainActor on state-changing methods.</action>
  <verify>MeetingState compiles, all methods marked @MainActor, follows @Observable pattern exactly</verify>
  <done>MeetingState provides centralized meeting management with reactive updates</done>
</task>

<task type="auto">
  <name>Task 2: Integrate MeetingState into RootView and environment</name>
  <files>MeetingManager/Views/RootView.swift</files>
  <action>Add @State var meetingState = MeetingState() to RootView alongside existing organizationState. Inject into environment chain with .environment(meetingState) after .environment(organizationState). This makes MeetingState available throughout the view hierarchy. No other changes needed - RootView already has the established pattern for state management. Follow exact pattern from OrganizationState integration in Phase 3.</action>
  <verify>xcodebuild succeeds, meetingState injected into environment, available to all child views</verify>
  <done>MeetingState accessible throughout app via @Environment</done>
</task>

<task type="auto">
  <name>Task 3: Update views to use MeetingState for operations</name>
  <files>MeetingManager/Views/Meeting/MeetingListView.swift, MeetingManager/Views/Meeting/MeetingDetailView.swift</files>
  <action>Update MeetingListView: Add @Environment(MeetingState.self) private var meetingState. Change .task to call meetingState.loadMeetings(organization.id) instead of direct service call. Use meetingState.meetings for list data instead of @State meetings. Update CreateMeetingView integration to call meetingState.createMeeting and refresh list automatically. Update MeetingDetailView: Add @Environment(MeetingState.self). Change CheckInButton to call meetingState.checkIn instead of direct service call. Start/End meeting buttons call meetingState.startMeeting/endMeeting. Remove local refreshedMeeting state - use meetingState.meetings.first(where: {$0.id == meeting.id}) for reactive updates. Both views benefit from automatic updates when state changes anywhere in the app.</action>
  <verify>Views compile, use MeetingState for all operations, updates propagate automatically across views</verify>
  <done>Meeting views integrated with MeetingState, provide reactive updates across the app</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] MeetingState follows @Observable pattern with @MainActor methods
- [ ] RootView injects MeetingState into environment
- [ ] MeetingListView and MeetingDetailView use MeetingState for operations
- [ ] State changes propagate automatically across views
- [ ] No compiler errors or warnings
</verification>

<success_criteria>

- All tasks completed
- MeetingState provides centralized meeting management
- Environment injection enables app-wide state access
- Meeting views reactive to state changes
- Consistent with OrganizationState pattern from Phase 3
- Phase 4 complete: Full meeting management with creation, scheduling, check-in, and attendance tracking
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/04-meeting-management/04-04-SUMMARY.md`
</output>
