---
phase: 04-meeting-management
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified: [MeetingManager/Views/Meeting/MeetingListView.swift, MeetingManager/Views/Meeting/MeetingDetailView.swift, MeetingManager/Views/Organization/OrganizationDetailView.swift, MeetingManager.xcodeproj/project.pbxproj]
autonomous: true
---

<objective>
Create meeting list and detail views for browsing and viewing meetings.

Purpose: Enable members to see organization meetings (upcoming, active, past) and view meeting details including attendance.
Output: MeetingListView with categorized meetings, MeetingDetailView with full meeting information, navigation integration from OrganizationDetailView.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-meeting-management/04-01-SUMMARY.md
@MeetingManager/MeetingManager/Models/Meeting.swift
@MeetingManager/MeetingManager/Services/MeetingService.swift
@MeetingManager/MeetingManager/Views/Organization/OrganizationDetailView.swift
@MeetingManager/MeetingManager/Views/Organization/MemberListView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MeetingListView with categorized sections</name>
  <files>MeetingManager/Views/Meeting/MeetingListView.swift, MeetingManager.xcodeproj/project.pbxproj</files>
  <action>Create MeetingListView accepting organization: Organization parameter. @State for meetings: [Meeting], isLoading, errorMessage. Load meetings in .task(id: organization.id) using MeetingService().fetchMeetingsForOrganization(). Group meetings into three sections using computed properties: upcoming (isUpcoming), active (isActive), past (isPast). List with three sections: "Active" first, "Upcoming" second, "Past" third. Each section header with .font(.headline). Meeting row: NavigationLink to MeetingDetailView, display meeting.title, formatted scheduledAt (relative: "Today 3pm", "Tomorrow 9am", "Jan 20 2pm"), attendance count badge "X checked in". Active meetings show green indicator dot. Empty state when no meetings: "No meetings scheduled" with suggestion "Tap + to create a meeting". Toolbar with + button (sheet presentation of CreateMeetingView, only visible to admins). Handle loading state with ProgressView, error state with inline Text. Follow MemberListView pattern from Phase 3.</action>
  <verify>xcodebuild succeeds, MeetingListView compiles, groups meetings correctly by state</verify>
  <done>Members see meetings categorized by status, can navigate to details, admins see create button</done>
</task>

<task type="auto">
  <name>Task 2: Create MeetingDetailView with attendance and actions</name>
  <files>MeetingManager/Views/Meeting/MeetingDetailView.swift, MeetingManager.xcodeproj/project.pbxproj</files>
  <action>Create MeetingDetailView accepting meeting: Meeting, currentUserId: UUID parameters. @State for refreshedMeeting (copy of meeting for real-time updates after check-in). ScrollView with VStack sections: (1) Header: meeting.title .font(.largeTitle), status badge (Active/Upcoming/Past), (2) Details: LabeledContent rows for "Scheduled", "Started", "Ended" (format dates, show "Not started" / "Ongoing" when nil), (3) Attendance: section header with count "X of Y members checked in", List of attendee names (fetch User records for attendeeIds using Supabase, show email as fallback), (4) CheckInButton if meeting.isActive and !meeting.attendeeIds.contains(currentUserId), (5) Actions (admin only): "Start Meeting" button (updates started_at to now), "End Meeting" button (updates ended_at to now), both disabled if invalid state. Use .refreshable to reload meeting data. Follow OrganizationDetailView pattern with LabeledContent for consistent design. Update refreshedMeeting after check-in/state changes.</action>
  <verify>xcodebuild succeeds, detail view shows all meeting info, check-in button appears for active meetings</verify>
  <done>Members view meeting details and attendance, can check in when active, admins can start/end meetings</done>
</task>

<task type="auto">
  <name>Task 3: Add meeting navigation to OrganizationDetailView</name>
  <files>MeetingManager/Views/Organization/OrganizationDetailView.swift</files>
  <action>Add "Meetings" section to OrganizationDetailView after member count row. NavigationLink with label "View Meetings" and chevron, destination: MeetingListView(organization: organization). Use LabeledContent pattern: label "Meetings", value as NavigationLink. Position below "Members" section, before other sections. Ensure consistent styling with existing detail rows. No state changes needed, just navigation addition.</action>
  <verify>xcodebuild succeeds, "Meetings" link appears in organization detail, tapping navigates to meeting list</verify>
  <done>Organization detail provides direct access to meeting list view</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] MeetingListView categorizes meetings into Active/Upcoming/Past sections
- [ ] MeetingDetailView displays full meeting information with attendance
- [ ] Navigation flows from OrganizationDetailView → MeetingListView → MeetingDetailView
- [ ] Admin-only buttons (Create/Start/End) properly gated by organization.isAdmin check
- [ ] No compiler errors or warnings
</verification>

<success_criteria>

- All tasks completed
- Meeting list shows categorized meetings with navigation
- Meeting detail displays comprehensive information
- Check-in button integrated into detail view
- Admin actions (start/end meeting) available in detail view
- Navigation integrated into organization flow
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/04-meeting-management/04-03-SUMMARY.md`
</output>
