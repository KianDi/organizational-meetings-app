---
phase: 04-meeting-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [MeetingManager/Services/MeetingService.swift, MeetingManager/Views/Meeting/CheckInButton.swift, MeetingManager.xcodeproj/project.pbxproj]
autonomous: true
---

<objective>
Implement simple check-in system for meeting attendance tracking.

Purpose: Enable members to check in to active meetings with a single button tap, recording attendance in real-time.
Output: Check-in method in MeetingService, CheckInButton SwiftUI component with instant feedback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@MeetingManager/MeetingManager/Models/Meeting.swift
@MeetingManager/MeetingManager/Models/User.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add check-in method to MeetingService</name>
  <files>MeetingManager/Services/MeetingService.swift</files>
  <action>Add checkIn(meetingId: UUID, userId: UUID) async throws method to MeetingService actor. Fetch current meeting, verify userId not already in attendeeIds (early return if duplicate for idempotency), append userId to attendeeIds array, update database with PATCH. Use Postgrest .update() with .eq("id", value: meetingId). Only update attendee_ids column to avoid race conditions with other fields. Return updated Meeting model. Throw descriptive error if meeting not found or update fails. Follow async/await pattern from existing methods.</action>
  <verify>Method compiles, handles duplicate check-ins gracefully, updates only attendee_ids field</verify>
  <done>MeetingService.checkIn prevents duplicates and updates attendance atomically</done>
</task>

<task type="auto">
  <name>Task 2: Create CheckInButton component with state management</name>
  <files>MeetingManager/Views/Meeting/CheckInButton.swift, MeetingManager.xcodeproj/project.pbxproj</files>
  <action>Create CheckInButton view accepting meeting: Meeting, userId: UUID parameters. @State for isCheckedIn (computed from meeting.attendeeIds.contains(userId)), isSubmitting, errorMessage. Button with text "Check In" or "Checked In ✓" based on state. Disabled when isCheckedIn or isSubmitting. On tap: call MeetingService().checkIn(meetingId, userId), show loading spinner in button, update local isCheckedIn state on success, display inline error below button on failure. Button styling: .buttonStyle(.borderedProminent) when not checked in, .buttonStyle(.bordered) when checked in. Green tint for checked-in state. Add haptic feedback on successful check-in (UINotificationFeedbackGenerator().notificationOccurred(.success)). Auto-dismiss error after 3 seconds using Task.sleep.</action>
  <verify>xcodebuild succeeds, button renders correctly in both states, prevents duplicate taps</verify>
  <done>Members can check in with single tap, immediate visual feedback, prevents double check-ins</done>
</task>

<task type="auto">
  <name>Task 3: Add check-in error handling and edge cases</name>
  <files>MeetingManager/Services/MeetingService.swift</files>
  <action>Enhance checkIn method with validation: verify meeting.isActive (started but not ended), throw MeetingError.notStarted if startedAt is nil, throw MeetingError.alreadyEnded if endedAt is not nil, throw MeetingError.notAMember if userId not in organization's memberIds (requires fetching organization). Define enum MeetingError: Error with cases notStarted, alreadyEnded, notAMember, each with localized descriptions. Check these conditions before database update to prevent invalid check-ins. Return clear error messages for UI display.</action>
  <verify>Invalid check-in attempts throw appropriate errors, only active meetings allow check-in</verify>
  <done>Check-in validates meeting state and membership before allowing attendance recording</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] MeetingService.checkIn handles duplicates and validates meeting state
- [ ] CheckInButton provides instant feedback and prevents double-taps
- [ ] Edge cases covered (meeting not started, already ended, not a member)
- [ ] No compiler errors or warnings
</verification>

<success_criteria>

- All tasks completed
- Check-in is idempotent and validates state
- Button component provides clear visual feedback
- Error handling covers edge cases
- No errors or warnings introduced
  </success_criteria>

<output>
After completion, create `.planning/phases/04-meeting-management/04-02-SUMMARY.md`
</output>
