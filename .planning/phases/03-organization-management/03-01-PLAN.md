---
phase: 03-organization-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [MeetingManager/Services/OrganizationService.swift, MeetingManager/Views/Organization/CreateOrganizationView.swift]
autonomous: true
---

<objective>
Implement organization creation flow with Supabase backend integration.

Purpose: Allow authenticated users to create new organizations and become admins
Output: Working organization creation with backend persistence and UI
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-system/02-04-SUMMARY.md
@MeetingManager/MeetingManager/Models/Organization.swift
@MeetingManager/MeetingManager/Models/User.swift
@MeetingManager/MeetingManager/Auth/AuthState.swift
@MeetingManager/MeetingManager/Auth/SupabaseConfig.swift

**Available from Phase 2:**
- Supabase Swift SDK 2.40.0 integrated
- Actor-based patterns for thread-safe operations
- @Observable state management pattern
- Environment-based dependency injection
- AuthState with currentUser access

**Data model from Phase 1:**
- Organization model with id, name, adminId, createdAt, inviteCode, memberIds
- UUID-based relationships for clean Codable serialization
- Admin auto-included in memberIds for consistency
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OrganizationService actor</name>
  <files>MeetingManager/Services/OrganizationService.swift</files>
  <action>
Create actor OrganizationService following Phase 2's AuthService pattern:
- Actor isolation for thread-safe database operations
- Use SupabaseClient from SupabaseConfig.shared
- Implement createOrganization(name: String, adminId: UUID) async throws -> Organization
  - Generate UUID for org
  - Create inviteCode (6-char alphanumeric using uppercased UUID prefix)
  - Set memberIds to [adminId] (admin auto-included)
  - Insert into Supabase "organizations" table
  - Return Organization model
- Use Supabase Postgrest API: supabase.from("organizations").insert(...)
- Throw errors for network/database failures
- Follow established pattern: actor with private supabase property
  </action>
  <verify>Build succeeds with no errors, OrganizationService compiles</verify>
  <done>OrganizationService actor exists with createOrganization method</done>
</task>

<task type="auto">
  <name>Task 2: Create organization creation UI</name>
  <files>MeetingManager/Views/Organization/CreateOrganizationView.swift</files>
  <action>
Create SwiftUI view for organization creation:
- @State private var organizationName: String = ""
- @State private var errorMessage: String?
- @State private var isCreating: Bool = false
- Get AuthState from environment (@Environment)
- Form with TextField for organization name
- Disable submit if organizationName.isEmpty or isCreating
- Submit button calls async createOrganization()
  - Set isCreating = true
  - Call OrganizationService().createOrganization(name: organizationName, adminId: currentUser.id)
  - On success: dismiss view or navigate to org detail
  - On failure: set errorMessage
  - Always set isCreating = false in defer block
- Display errorMessage if present
- Follow Phase 2's LoginView/SignupView pattern for form validation and async submission
- Use NavigationStack with toolbar for Cancel button
  </action>
  <verify>Build succeeds, view renders in simulator</verify>
  <done>CreateOrganizationView allows users to create organizations</done>
</task>

<task type="auto">
  <name>Task 3: Add Supabase organizations table schema</name>
  <files>MeetingManager/Database/schema.sql</files>
  <action>
Create SQL schema file for Supabase organizations table:
- Table: organizations
- Columns:
  - id UUID PRIMARY KEY DEFAULT uuid_generate_v4()
  - name TEXT NOT NULL
  - admin_id UUID NOT NULL REFERENCES auth.users(id)
  - created_at TIMESTAMPTZ DEFAULT now()
  - invite_code TEXT UNIQUE
  - member_ids UUID[] NOT NULL DEFAULT '{}'
- Enable Row Level Security (RLS)
- Policy: Users can read organizations they're a member of (member_ids @> ARRAY[auth.uid()])
- Policy: Admins can update their own organizations (admin_id = auth.uid())
- Policy: Any authenticated user can insert (create) organizations
- Add comment explaining this file should be run in Supabase SQL Editor
  </action>
  <verify>SQL file exists with valid PostgreSQL syntax</verify>
  <done>Organizations table schema ready for Supabase deployment</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds with no Swift errors
- [ ] OrganizationService compiles with actor isolation
- [ ] CreateOrganizationView renders in preview
- [ ] SQL schema is valid PostgreSQL
</verification>

<success_criteria>

- OrganizationService actor created following AuthService pattern
- CreateOrganizationView form with validation and async submission
- SQL schema file ready for Supabase deployment
- No errors or warnings introduced
- Code follows established Phase 2 patterns (actor, @Observable, environment injection)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-organization-management/03-01-SUMMARY.md`
</output>
