---
phase: 03-organization-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [MeetingManager/Services/OrganizationService.swift, MeetingManager/Views/Organization/JoinOrganizationView.swift]
autonomous: true
---

<objective>
Implement invite code joining flow with validation and member addition.

Purpose: Allow users to join existing organizations using invite codes
Output: Working join flow with invite code validation and membership tracking
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-system/02-04-SUMMARY.md
@MeetingManager/MeetingManager/Models/Organization.swift
@MeetingManager/MeetingManager/Models/User.swift
@MeetingManager/MeetingManager/Auth/AuthState.swift

**Available from Phase 2:**
- Supabase Swift SDK with Postgrest API
- Actor-based service patterns
- @Observable state management
- Environment dependency injection

**Plan 03-01 creates (runs in parallel):**
- OrganizationService actor (we'll extend it)
- SQL schema with invite_code column
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add join methods to OrganizationService</name>
  <files>MeetingManager/Services/OrganizationService.swift</files>
  <action>
Extend OrganizationService actor with join functionality:
- Add findByInviteCode(code: String) async throws -> Organization?
  - Query: supabase.from("organizations").select().eq("invite_code", code).single()
  - Return Organization if found, nil if not found
  - Throw on database errors
- Add joinOrganization(organizationId: UUID, userId: UUID) async throws -> Organization
  - Fetch current organization
  - Check if userId already in memberIds (if yes, return early - already a member)
  - Append userId to memberIds array
  - Update: supabase.from("organizations").update(["member_ids": updatedMemberIds]).eq("id", organizationId)
  - Return updated Organization
- Add updateUserOrganizations(userId: UUID, organizationId: UUID) async throws
  - Fetch user's current organization_ids from "users" table
  - Append organizationId if not present
  - Update: supabase.from("users").update(["organization_ids": updatedOrgIds]).eq("id", userId)
- Use PostgreSQL array append syntax for member_ids updates
  </action>
  <verify>Build succeeds, new methods compile without errors</verify>
  <done>OrganizationService has findByInviteCode, joinOrganization, and updateUserOrganizations methods</done>
</task>

<task type="auto">
  <name>Task 2: Create join organization UI</name>
  <files>MeetingManager/Views/Organization/JoinOrganizationView.swift</files>
  <action>
Create SwiftUI view for joining via invite code:
- @State private var inviteCode: String = ""
- @State private var errorMessage: String?
- @State private var isJoining: Bool = false
- Get AuthState from environment
- Form with TextField for invite code (uppercased, 6 chars max)
- Disable submit if inviteCode.count != 6 or isJoining
- Submit button calls async joinWithCode()
  - Set isJoining = true
  - Call OrganizationService().findByInviteCode(code: inviteCode)
  - If not found: set errorMessage = "Invalid invite code"
  - If found: call joinOrganization(organizationId: org.id, userId: currentUser.id)
  - Then call updateUserOrganizations(userId: currentUser.id, organizationId: org.id)
  - On success: dismiss or navigate to org detail
  - On failure: set errorMessage with error description
  - Always set isJoining = false in defer block
- Display errorMessage if present
- TextField modifier: .textInputAutocapitalization(.characters) for uppercase
- Follow Phase 2's form validation pattern
  </action>
  <verify>Build succeeds, view renders correctly</verify>
  <done>JoinOrganizationView allows joining via invite code with validation</done>
</task>

<task type="auto">
  <name>Task 3: Add users table schema for organization tracking</name>
  <files>MeetingManager/Database/schema.sql</files>
  <action>
Extend schema.sql with users table:
- Table: users (extends Supabase auth.users)
- Columns:
  - id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE
  - email TEXT NOT NULL
  - name TEXT NOT NULL
  - created_at TIMESTAMPTZ DEFAULT now()
  - organization_ids UUID[] NOT NULL DEFAULT '{}'
- Enable Row Level Security (RLS)
- Policy: Users can read their own user record (id = auth.uid())
- Policy: Users can update their own record (id = auth.uid())
- Add trigger to auto-create users row when auth.users row is created
- Add comment explaining RLS policies
  </action>
  <verify>SQL syntax is valid PostgreSQL</verify>
  <done>Users table schema ready with RLS policies</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds with no errors
- [ ] OrganizationService has join methods
- [ ] JoinOrganizationView form works with uppercase input
- [ ] SQL schema includes users table with RLS
</verification>

<success_criteria>

- OrganizationService extended with findByInviteCode and joinOrganization
- JoinOrganizationView with invite code validation (6 chars, uppercase)
- Users table schema with organization_ids tracking
- No errors or warnings introduced
- Follows established actor and form patterns
  </success_criteria>

<output>
After completion, create `.planning/phases/03-organization-management/03-02-SUMMARY.md`
</output>
