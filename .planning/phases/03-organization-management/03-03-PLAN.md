---
phase: 03-organization-management
plan: 03
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified: [MeetingManager/Services/OrganizationService.swift, MeetingManager/Views/Organization/OrganizationDetailView.swift, MeetingManager/Views/Organization/MemberListView.swift]
autonomous: true
---

<objective>
Build member list UI with role display and basic management.

Purpose: Show organization members with admin designation and provide member viewing
Output: Member list view displaying all members with role indicators
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-organization-management/03-01-SUMMARY.md
@.planning/phases/03-organization-management/03-02-SUMMARY.md
@MeetingManager/MeetingManager/Models/Organization.swift
@MeetingManager/MeetingManager/Models/User.swift
@MeetingManager/MeetingManager/Auth/AuthState.swift

**Available from Plans 03-01 and 03-02:**
- OrganizationService with create and join methods
- Supabase organizations and users tables
- CreateOrganizationView and JoinOrganizationView

**Data model:**
- Organization has adminId and memberIds array
- Organization.isAdmin(userId:) method for role checking
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add member fetching to OrganizationService</name>
  <files>MeetingManager/Services/OrganizationService.swift</files>
  <action>
Extend OrganizationService actor with member queries:
- Add fetchOrganization(id: UUID) async throws -> Organization
  - Query: supabase.from("organizations").select().eq("id", id).single()
  - Decode and return Organization model
  - Throw if not found or database error
- Add fetchMembers(organizationId: UUID) async throws -> [User]
  - First fetch Organization to get memberIds
  - Query users: supabase.from("users").select().in("id", memberIds)
  - Decode array of User models
  - Return sorted by name
  - Throw on database errors
- Use Postgrest .in() filter for array membership query
  </action>
  <verify>Build succeeds, methods compile without errors</verify>
  <done>OrganizationService can fetch organizations and their members</done>
</task>

<task type="auto">
  <name>Task 2: Create member list view</name>
  <files>MeetingManager/Views/Organization/MemberListView.swift</files>
  <action>
Create SwiftUI view for displaying organization members:
- Accept organization: Organization as parameter
- @State private var members: [User] = []
- @State private var isLoading: Bool = false
- @State private var errorMessage: String?
- Get AuthState from environment
- List displaying members:
  - ForEach(members) with HStack containing:
    - VStack(alignment: .leading) with name and email
    - Spacer()
    - If user.id == organization.adminId: Text("Admin").font(.caption).foregroundColor(.secondary)
  - Sort with admin first, then alphabetically by name
- .task { await loadMembers() } on appear
- loadMembers() async method:
  - Set isLoading = true
  - Call OrganizationService().fetchMembers(organizationId: organization.id)
  - Set members on success
  - Set errorMessage on failure
  - Always set isLoading = false in defer block
- Show ProgressView if isLoading
- Show error message if errorMessage exists
- Navigation title: "Members (\(members.count))"
  </action>
  <verify>Build succeeds, view renders in preview</verify>
  <done>MemberListView displays organization members with admin indicator</done>
</task>

<task type="auto">
  <name>Task 3: Create organization detail view</name>
  <files>MeetingManager/Views/Organization/OrganizationDetailView.swift</files>
  <action>
Create SwiftUI view for organization overview:
- Accept organizationId: UUID as parameter
- @State private var organization: Organization?
- @State private var isLoading: Bool = false
- @State private var errorMessage: String?
- Get AuthState from environment
- List with sections:
  - Section "Details":
    - LabeledContent("Name", value: organization.name)
    - LabeledContent("Invite Code", value: organization.inviteCode ?? "N/A")
    - LabeledContent("Created", value: formatted createdAt date)
  - Section "Members":
    - NavigationLink to MemberListView(organization: organization)
    - Text("View all members").foregroundColor(.secondary)
- .task { await loadOrganization() } on appear
- loadOrganization() async:
  - Set isLoading = true
  - Call OrganizationService().fetchOrganization(id: organizationId)
  - Set organization on success
  - Set errorMessage on failure
  - Always set isLoading = false in defer block
- Show ProgressView if isLoading
- Show error if errorMessage exists
- Navigation title: organization?.name ?? "Organization"
- Toolbar with share button to share invite code (if user is admin)
  </action>
  <verify>Build succeeds, view layout looks correct</verify>
  <done>OrganizationDetailView shows org info with navigation to members</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds with no errors
- [ ] OrganizationService has fetchOrganization and fetchMembers
- [ ] MemberListView displays members with admin badge
- [ ] OrganizationDetailView shows org details and links to members
</verification>

<success_criteria>

- OrganizationService can fetch organizations and members
- MemberListView displays sorted members with admin designation
- OrganizationDetailView provides organization overview
- No errors or warnings introduced
- Views follow SwiftUI best practices with .task for loading
  </success_criteria>

<output>
After completion, create `.planning/phases/03-organization-management/03-03-SUMMARY.md`
</output>
